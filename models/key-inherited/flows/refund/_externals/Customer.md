---
external: Customer
---

The buyer who originally placed the order and is now requesting a refund.
Modelled as an external entity — the actor who initiates the return request —
distinct from the `Party` store that records their details.
