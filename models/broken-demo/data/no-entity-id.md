---
group: core
pk:
  - shipment_id
columns:
  shipment_id:
    type: integer
---

This file has valid YAML but no `entity:` field. Triggers `parse.missing_id` (Class B). The file is skipped and a global error is recorded with the file path.
