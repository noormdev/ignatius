---
label: Grants
entities:
  - GrantAlpha
  - GrantBeta
---

Grants issued together; read by Process Two as plain `db:` entries in this
fixture so the pipeline still has a `clusters/` file to group by in the
connected view.
