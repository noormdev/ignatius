name: CI

on:
  push:
    branches: ["**"]
  pull_request:
    branches: [master, main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Cache Playwright browsers
        id: playwright-cache
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ runner.os }}-${{ hashFiles('**/bun.lock') }}
          restore-keys: |
            playwright-${{ runner.os }}-

      - name: Install Playwright Chromium (headless shell)
        if: steps.playwright-cache.outputs.cache-hit != 'true'
        run: bunx playwright install chromium-headless-shell --with-deps

      - name: Build React bundle (stage A)
        run: bun run build:bundle

      - name: Rename to stable names (stage A.1)
        run: bun run build:stable-names

      - name: Compile binary (stage B)
        run: bun build --compile src/cli.ts --outfile dist/ignatius

      - name: Run test scripts
        run: |
          set -e
          bun tmp/test-cli-parse.ts
          bun tmp/test-theme-parse.ts
          bun tmp/test-dict-gen.ts
          bun tmp/test-graph-gen.ts
          bun tmp/test-sse-live-reload.ts
          bun tmp/test-cli-binary.ts

      - name: Typecheck
        run: bunx tsc --noEmit
        continue-on-error: true  # pre-existing errors documented in signals.md
